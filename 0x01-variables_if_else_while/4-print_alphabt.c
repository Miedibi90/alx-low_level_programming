#include <stdio.h>
/**
 * main - my Entry point; code to print lower case
 *
 * Return: Always 0 (Successful)
 */

int main(void)
{
	int i = 'a';

	while (i = 'a')
	{
		if (i != 'q' && i != 'e')
		{
			putchar(i);
		}
		i++;
	}
	putchar('\n');
	return (0);
}
